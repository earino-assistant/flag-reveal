{
  "name": "flag-reveal",
  "version": "0.1.18",
  "private": true,
  "description": "Flag Reveal — a Jackbox-style flag-guessing party game. Static, no-build, Firebase RTDB sync.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "check": "node --check config.js && for f in js/*.js tests/*.js tools/*.mjs; do [ -e \"$f\" ] || continue; node --check \"$f\" || exit 1; done"
  }
}
